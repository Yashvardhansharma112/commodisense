# CommodiSense Dashboard — Streamlit Cloud
# IMPORTANT: pandas-ta must be pinned to 0.3.14b0 — newer versions depend on numba
# which requires llvmlite which does NOT support Python 3.14 (Streamlit Cloud default).
# shap removed for same reason — all shap versions require numba.
# SHAP signals will show as empty until running locally or on Python 3.12.

duckdb>=0.10.0
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.0
requests>=2.28.0
xgboost>=2.0.0
lightgbm>=4.0.0
scikit-learn>=1.3.0
pandas-ta==0.3.14b0
groq>=0.4.0
streamlit>=1.28.0
plotly>=5.15.0
python-dotenv>=1.0.0
